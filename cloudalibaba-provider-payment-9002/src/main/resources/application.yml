server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  #配置nacos地址

#打开全部的监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'